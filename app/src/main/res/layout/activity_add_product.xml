<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:elevation="4dp">

        <ImageView
            android:id="@+id/btnBackAddProduct"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_back"
            android:padding="4dp"
            android:background="#F5F7FA"
            android:clickable="true"
            app:tint="@color/colorTextPrimary" />

        <TextView
            android:id="@+id/tvAddProductTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Thêm Sản Phẩm Mới"
            android:textColor="@color/colorTextPrimary"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginStart="16dp"/>
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <EditText
            android:id="@+id/edtAddProductName"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:hint="Tên sản phẩm (VD: Áo thun nam)"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:background="@drawable/bg_edittext"
            android:layout_marginBottom="16dp"/>

        <EditText
            android:id="@+id/edtAddProductPrice"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:hint="Giá bán (VD: 250.000 đ)"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:background="@drawable/bg_edittext"
            android:layout_marginBottom="16dp"/>

        <EditText
            android:id="@+id/edtAddProductImageUrl"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:hint="Dán đường link ảnh (URL) vào đây"
            android:gravity="top|start"
            android:padding="16dp"
            android:background="@drawable/bg_edittext"
            android:layout_marginBottom="32dp"/>

        <Button
            android:id="@+id/btnSaveNewProduct"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:text="ĐĂNG SẢN PHẨM"
            android:textColor="@color/colorSurface"
            android:textSize="16sp"
            android:textStyle="bold"
            android:background="@drawable/bg_button"
            app:backgroundTint="@null" />
    </LinearLayout>
</LinearLayout>